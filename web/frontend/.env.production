# TDL Web Frontend Production Environment

# API Configuration
VITE_API_URL=/api/v1

# Global Proxy Configuration (for Telegram API)
# Set this to your proxy server for production
VITE_GLOBAL_PROXY=http://127.0.0.1:7890

# Production Configuration
VITE_NODE_ENV=production

# Backend Configuration (for production, usually same origin)
VITE_BACKEND_URL=

# WebSocket Configuration (for production, usually same origin)
VITE_WS_URL=/ws